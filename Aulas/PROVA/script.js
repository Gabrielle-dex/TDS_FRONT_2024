//Ler valores nome e email | input
const mensagem = document.getElementsById("mensagem_confirmacao");

function mensagem_confirmacao (mensagem_confirmacao){

 if (button.innerHTML == onclick){

    console.log("Nome caputurado");
    console.log("Email capturado");
 
    mensagem.innerHTML = `Muito obrigado(a), senhor(a) : ${nome_capturado_input}
    Acabamos de enviar um e-mail para: ${email_capturado_input}.`;
    return true;
 }

}